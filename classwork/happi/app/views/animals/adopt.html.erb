<% if @animal.errors.any? %>
  <div id="error_explanation">
    <p><%= pluralize(@animal.errors.count, "error") %> prohibited this animal from being saved:</p>
 
    <ul>
    <% @animal.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
<h2>Adopt <%= @animal.name %> </h2>

<%= form_for([@shelter, @animal], url: { action: 'complete_adoption' }, html: { method: 'put' }) do |f| %>
  <p>Client to adopt <%= @animal.name %>: <%= f.collection_select(:client, Client.where(shelter: @shelter), :id, :name)  %></p>
  <p><%= f.submit %></p>
<% end %>